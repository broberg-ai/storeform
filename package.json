{
  "name": "storeform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Selvhelende, schema-drevet formular-automation for App Store Connect & Google Play Console — driver cloud Lens (aldrig rå Playwright).",
  "module": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "serve-fixture": "bun run src/serve-fixture.ts"
  },
  "dependencies": {
    "@broberg/lens-client": "0.1.0",
    "yaml": "2.9.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "6.0.3"
  }
}
